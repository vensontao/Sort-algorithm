#include <stdio.h>

//---------------------------------------------
//  通过变量指针，实现交换
//---------------------------------------------
void swap(int *a ,int *b)
{
    int tmp;
    tmp=*a;
    *a=*b;
    *b=tmp;
}

//---------------------------------------------
// 通过变量引用，实现交换
//---------------------------------------------
void swap(int &a, int &b)
{
    int tmp;
    tmp=a;
    a=b;
    b=tmp;
}

//---------------------------------------------
//  加减运算，实现数据交换
//---------------------------------------------
void swap(int* a, int* b)
{
    *a = *a+*b;
    *b = *a-*b;
    *a = *a-*b;
}

//---------------------------------------------
// 加减运算，实现数据交换
//---------------------------------------------
void swap(int &a, int &b)
{
    a=a+b;
    b=a-b;
    a=a-b;
}

//---------------------------------------------
//  乘除运算，实现数据交换
//---------------------------------------------
void swap(int &a,  int  &b)
{
    a=a*b;
    b=a/b;
    a=a/b;
}

//---------------------------------------------
//  异或运算，实现数据交换
//---------------------------------------------
void swap(int &a, int &b)
{
    a=a^b;
    b=a^b;
    a=a^b;
}

//---------------------------------------------
// 主函数测试
//---------------------------------------------
int main()
{
    int num1=5,num2=10,tmp=0;
    printf("before:\n");
    printf("num1=%d\num2=%d\n",num1,num2);
    swap(num1, num2);
    printf("after:\n");
    printf("num1=%d\num2=%d\n",num1,num2);
    return 0;
}
